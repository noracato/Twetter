= form_with(model: twet, local: true) do |form|
  - if twet.errors.any?
    #error_explanation
      %h2
        = pluralize(twet.errors.count, "error")
        prohibited this twet from being saved:
      %ul
        - twet.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :message
    = form.text_field :message
  .actions
    = form.submit
